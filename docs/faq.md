# FAQ

**Q: What transports are supported?**
A: WebSocket by default. Custom transports can be implemented.

**Q: How do I handle errors?**
A: Use `client.on('error', handler)`.

**Q: How do I reconnect automatically?**
A: Set `reconnect: true` in the client options.

